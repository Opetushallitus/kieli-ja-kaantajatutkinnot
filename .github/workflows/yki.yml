name: YKI

on:
  workflow_dispatch:
  push:
    paths:
      - "backend/yki/**"
      - "backend/pom.xml"
      - "frontend/packages/yki/**"
      - "frontend/yarn.lock"
      - "!**/*.md"
    # Setting 'branches' has the side effect that just pushing tags does not start workflows.
    branches:
      - '**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  public-frontend:
    uses: ./.github/workflows/yki-public-frontend.yml
    with:
      app-name: "yki"
      cypress-base-url: "http://localhost:4003"
  clerk-frontend:
    uses: ./.github/workflows/yki-clerk-frontend.yml
    with:
      app-name: "yki"
      cypress-base-url: "http://localhost:4004"
  backend:
    needs: [public-frontend, clerk-frontend]
    uses: ./.github/workflows/common-backend.yml
    with:
      app-name: "yki"
  deploy:
    needs: backend
    uses: ./.github/workflows/common-deploy.yml
    with:
      app-name: "yki"
      image-name: "yki-ui"
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
